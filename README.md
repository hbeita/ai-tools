# ai-tools

Configs, scripts, and settings for AI tools.

## Claude

- [`claude/statusline.sh`](claude/statusline.sh) — Custom Claude Code status line showing model, context window usage, git branch, dirty file count, autorun state, session, and worktree.
